---
interface Props {
  /** Überschrift des Abschnitts. */
  title: string;
}

const { title } = Astro.props;
---

<section class="shadow-soft rounded-xl border border-border bg-card p-6">
  <h2 class="text-xl font-semibold tracking-tight text-balance">{title}</h2>
  <slot />
</section>
