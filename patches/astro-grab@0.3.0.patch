diff --git a/dist/client/auto.js b/dist/client/auto.js
index 90feeab38a4f381f007d3c0fc7ad1b687fb25d22..032d5750d916fbefb7838e8ff765078f208099b1 100644
--- a/dist/client/auto.js
+++ b/dist/client/auto.js
@@ -67,7 +67,6 @@ var KeybindHandler = class {
   holdDuration;
   triggerKey;
   stateMachine;
-  hasActivatedOnce = false;
   currentMouseX = 0;
   currentMouseY = 0;
   constructor(stateMachine, holdDuration = 1e3, triggerKey = DEFAULT_TRIGGER_KEY) {
@@ -94,23 +93,26 @@ var KeybindHandler = class {
     if (!isTriggerKey) {
       return;
     }
-    e.preventDefault();
+    // Auto-repeat only happens while the key is already down, so the first
+    // press has run whatever default action it deserved.
     if (e.repeat) {
+      e.preventDefault();
       return;
     }
-    const currentState = this.stateMachine.getState();
-    if (currentState !== "idle") {
+    if (this.stateMachine.getState() !== "idle") {
+      // Holding or targeting: the shortcut belongs to astro-grab.
+      e.preventDefault();
       return;
     }
-    if (this.hasActivatedOnce) {
+    // The default action is left intact: whether this press is a hold is only
+    // known once holdDuration elapses, long after the browser would have run
+    // its copy. Suppressing it here would break Cmd/Ctrl+C for every short
+    // press, so a hold copies the selection first and overwrites the clipboard
+    // with the snippet on grab.
+    this.stateMachine.transition("holding");
+    this.holdTimer = window.setTimeout(() => {
       this.stateMachine.transition("targeting");
-    } else {
-      this.stateMachine.transition("holding");
-      this.holdTimer = window.setTimeout(() => {
-        this.stateMachine.transition("targeting");
-        this.hasActivatedOnce = true;
-      }, this.holdDuration);
-    }
+    }, this.holdDuration);
   };
   handleKeyUp = (e) => {
     const pressedKey = parseTriggerKey(e.key);
@@ -458,15 +460,45 @@ var Overlay = class {
 };
 
 // src/shared/types.ts
-var DEFAULT_TEMPLATE = `Source: {{file}}:{{targetLine}}
+var DEFAULT_TEMPLATE = `Source: {{trace}}
 
 \`\`\`{{language}}
 {{snippet}}
 \`\`\``;
 
+// src/client/trace.ts
+var DEFAULT_TRACE_LIMIT = 4;
+var parseTraceEntry = (encoded) => {
+  const columnSeparator = encoded.lastIndexOf(":");
+  const lineSeparator = encoded.lastIndexOf(":", columnSeparator - 1);
+  if (columnSeparator < 1 || lineSeparator < 1) {
+    return null;
+  }
+  return {
+    file: encoded.slice(0, lineSeparator),
+    line: encoded.slice(lineSeparator + 1, columnSeparator)
+  };
+};
+var buildComponentTrace = (element, limit = DEFAULT_TRACE_LIMIT) => {
+  const entries = [];
+  let current = element;
+  while (current && entries.length < limit) {
+    const encoded = current.getAttribute("data-astro-grab");
+    const entry = encoded ? parseTraceEntry(encoded) : null;
+    // Consecutive elements from one file are one component, so only its
+    // innermost element is kept: the run collapses to the element that was
+    // actually targeted, and each further entry is the file that renders it.
+    if (entry && entry.file !== entries[entries.length - 1]?.file) {
+      entries.push(entry);
+    }
+    current = current.parentElement;
+  }
+  return entries.map((entry) => `${entry.file}:${entry.line}`).join(" \u2192 ");
+};
+
 // src/client/clipboard.ts
 var formatSnippet = (data, template = DEFAULT_TEMPLATE) => {
-  return template.replace(/\{\{file\}\}/g, data.file).replace(/\{\{snippet\}\}/g, data.snippet).replace(/\{\{startLine\}\}/g, String(data.startLine)).replace(/\{\{endLine\}\}/g, String(data.endLine)).replace(/\{\{targetLine\}\}/g, String(data.targetLine)).replace(/\{\{language\}\}/g, data.language);
+  return template.replace(/\{\{file\}\}/g, data.file).replace(/\{\{snippet\}\}/g, data.snippet).replace(/\{\{startLine\}\}/g, String(data.startLine)).replace(/\{\{endLine\}\}/g, String(data.endLine)).replace(/\{\{targetLine\}\}/g, String(data.targetLine)).replace(/\{\{language\}\}/g, data.language).replace(/\{\{trace\}\}/g, data.trace || `${data.file}:${data.targetLine}`);
 };
 var copyToClipboard = async (text) => {
   try {
@@ -628,12 +660,13 @@ var TargetingHandler = class {
       const src = this.currentTarget.getAttribute("data-astro-grab");
       if (src) {
         const rect = this.currentTarget.getBoundingClientRect();
-        await this.fetchAndCopySnippet(src, rect);
+        await this.fetchAndCopySnippet(src, rect, this.currentTarget);
       }
     }
     this.stateMachine.reset();
   };
-  async fetchAndCopySnippet(src, rect) {
+  async fetchAndCopySnippet(src, rect, element) {
+    const trace = element ? buildComponentTrace(element) : "";
     const primaryEndpoint = this.apiBaseUrl ? `${this.apiBaseUrl}/snippet` : "/__astro_grab/snippet";
     const fallbackEndpoint = "/__astro_grab/snippet";
     const tryEndpoint = async (endpoint) => {
@@ -649,7 +682,7 @@ var TargetingHandler = class {
           return false;
         }
         const data = await response.json();
-        const formatted = formatSnippet(data, this.template);
+        const formatted = formatSnippet({ ...data, trace }, this.template);
         await copyToClipboard(formatted);
         console.log(
           `[astro-grab] Copied snippet from ${data.file}:${data.targetLine}`
diff --git a/dist/client/index.js b/dist/client/index.js
index d34965fcfd63147ab1e7c72a0b0e20c073f4fc61..891d6c04a7b9bfcf870b582a08156522792d4199 100644
--- a/dist/client/index.js
+++ b/dist/client/index.js
@@ -67,7 +67,6 @@ var KeybindHandler = class {
   holdDuration;
   triggerKey;
   stateMachine;
-  hasActivatedOnce = false;
   currentMouseX = 0;
   currentMouseY = 0;
   constructor(stateMachine, holdDuration = 1e3, triggerKey = DEFAULT_TRIGGER_KEY) {
@@ -94,23 +93,26 @@ var KeybindHandler = class {
     if (!isTriggerKey) {
       return;
     }
-    e.preventDefault();
+    // Auto-repeat only happens while the key is already down, so the first
+    // press has run whatever default action it deserved.
     if (e.repeat) {
+      e.preventDefault();
       return;
     }
-    const currentState = this.stateMachine.getState();
-    if (currentState !== "idle") {
+    if (this.stateMachine.getState() !== "idle") {
+      // Holding or targeting: the shortcut belongs to astro-grab.
+      e.preventDefault();
       return;
     }
-    if (this.hasActivatedOnce) {
+    // The default action is left intact: whether this press is a hold is only
+    // known once holdDuration elapses, long after the browser would have run
+    // its copy. Suppressing it here would break Cmd/Ctrl+C for every short
+    // press, so a hold copies the selection first and overwrites the clipboard
+    // with the snippet on grab.
+    this.stateMachine.transition("holding");
+    this.holdTimer = window.setTimeout(() => {
       this.stateMachine.transition("targeting");
-    } else {
-      this.stateMachine.transition("holding");
-      this.holdTimer = window.setTimeout(() => {
-        this.stateMachine.transition("targeting");
-        this.hasActivatedOnce = true;
-      }, this.holdDuration);
-    }
+    }, this.holdDuration);
   };
   handleKeyUp = (e) => {
     const pressedKey = parseTriggerKey(e.key);
@@ -458,15 +460,45 @@ var Overlay = class {
 };
 
 // src/shared/types.ts
-var DEFAULT_TEMPLATE = `Source: {{file}}:{{targetLine}}
+var DEFAULT_TEMPLATE = `Source: {{trace}}
 
 \`\`\`{{language}}
 {{snippet}}
 \`\`\``;
 
+// src/client/trace.ts
+var DEFAULT_TRACE_LIMIT = 4;
+var parseTraceEntry = (encoded) => {
+  const columnSeparator = encoded.lastIndexOf(":");
+  const lineSeparator = encoded.lastIndexOf(":", columnSeparator - 1);
+  if (columnSeparator < 1 || lineSeparator < 1) {
+    return null;
+  }
+  return {
+    file: encoded.slice(0, lineSeparator),
+    line: encoded.slice(lineSeparator + 1, columnSeparator)
+  };
+};
+var buildComponentTrace = (element, limit = DEFAULT_TRACE_LIMIT) => {
+  const entries = [];
+  let current = element;
+  while (current && entries.length < limit) {
+    const encoded = current.getAttribute("data-astro-grab");
+    const entry = encoded ? parseTraceEntry(encoded) : null;
+    // Consecutive elements from one file are one component, so only its
+    // innermost element is kept: the run collapses to the element that was
+    // actually targeted, and each further entry is the file that renders it.
+    if (entry && entry.file !== entries[entries.length - 1]?.file) {
+      entries.push(entry);
+    }
+    current = current.parentElement;
+  }
+  return entries.map((entry) => `${entry.file}:${entry.line}`).join(" \u2192 ");
+};
+
 // src/client/clipboard.ts
 var formatSnippet = (data, template = DEFAULT_TEMPLATE) => {
-  return template.replace(/\{\{file\}\}/g, data.file).replace(/\{\{snippet\}\}/g, data.snippet).replace(/\{\{startLine\}\}/g, String(data.startLine)).replace(/\{\{endLine\}\}/g, String(data.endLine)).replace(/\{\{targetLine\}\}/g, String(data.targetLine)).replace(/\{\{language\}\}/g, data.language);
+  return template.replace(/\{\{file\}\}/g, data.file).replace(/\{\{snippet\}\}/g, data.snippet).replace(/\{\{startLine\}\}/g, String(data.startLine)).replace(/\{\{endLine\}\}/g, String(data.endLine)).replace(/\{\{targetLine\}\}/g, String(data.targetLine)).replace(/\{\{language\}\}/g, data.language).replace(/\{\{trace\}\}/g, data.trace || `${data.file}:${data.targetLine}`);
 };
 var copyToClipboard = async (text) => {
   try {
@@ -628,12 +660,13 @@ var TargetingHandler = class {
       const src = this.currentTarget.getAttribute("data-astro-grab");
       if (src) {
         const rect = this.currentTarget.getBoundingClientRect();
-        await this.fetchAndCopySnippet(src, rect);
+        await this.fetchAndCopySnippet(src, rect, this.currentTarget);
       }
     }
     this.stateMachine.reset();
   };
-  async fetchAndCopySnippet(src, rect) {
+  async fetchAndCopySnippet(src, rect, element) {
+    const trace = element ? buildComponentTrace(element) : "";
     const primaryEndpoint = this.apiBaseUrl ? `${this.apiBaseUrl}/snippet` : "/__astro_grab/snippet";
     const fallbackEndpoint = "/__astro_grab/snippet";
     const tryEndpoint = async (endpoint) => {
@@ -649,7 +682,7 @@ var TargetingHandler = class {
           return false;
         }
         const data = await response.json();
-        const formatted = formatSnippet(data, this.template);
+        const formatted = formatSnippet({ ...data, trace }, this.template);
         await copyToClipboard(formatted);
         console.log(
           `[astro-grab] Copied snippet from ${data.file}:${data.targetLine}`
diff --git a/dist/index.js b/dist/index.js
index cda638c656d28b34c2fc093064d63c8dbb9b9243..ca9a4b3e7b5fd633199c6d84d5256c171ec8943c 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -1,6 +1,6 @@
-import { readFile } from 'fs/promises';
+import { readFile, realpath } from 'fs/promises';
 import { fileURLToPath } from 'url';
-import { resolve, dirname, join } from 'path';
+import { isAbsolute, relative, resolve, sep, dirname, join } from 'path';
 import { parse } from '@astrojs/compiler';
 
 // src/server/vite-plugin.ts
@@ -90,129 +90,111 @@ var extractSnippet = (content, targetLine, contextLines = 4) => {
 
 // src/server/parser.ts
 var instrumentAstroFile = async (code, filePath, root) => {
-  let ast;
   try {
-    ast = await parse(code, { position: true });
+    const ast = await parse(code, { position: true });
+    return instrumentAst(ast.ast, code, filePath, root);
   } catch (error) {
     console.error(`[astro-grab] Failed to parse ${filePath}:`, error);
     return { code };
   }
-  const lines = code.split("\n");
-  const getLineAndColumn = (offset) => {
-    let currentOffset = 0;
-    for (let i = 0; i < lines.length; i++) {
-      const lineLength = lines[i].length + 1;
-      const lineEnd = currentOffset + lineLength;
-      if (offset < lineEnd || i === lines.length - 1) {
-        const column = offset - currentOffset + 1;
-        return {
-          line: i + 1,
-          // 1-based
-          column
-        };
-      }
-      currentOffset += lineLength;
-    }
-    return { line: lines.length, column: 1 };
-  };
+};
+var instrumentAst = (ast, code, filePath, root) => {
+  const lineStartOffsets = getLineStartOffsets(code);
   const injections = [];
-  let normalizedPath = normalizePath(filePath);
-  if (root) {
-    const normalizedRoot = normalizePath(root);
-    if (normalizedPath.startsWith(normalizedRoot)) {
-      normalizedPath = normalizedPath.slice(normalizedRoot.length);
-      if (normalizedPath.startsWith("/")) {
-        normalizedPath = normalizedPath.slice(1);
-      }
+  const injectionOffsets = /* @__PURE__ */ new Set();
+  const normalizedPath = getRelativeFilePath(filePath, root);
+  const bodyNode = findBody(ast);
+  const rootToWalk = bodyNode ?? ast;
+  walkAst(rootToWalk, (node) => {
+    const isInspectableElement = node.type === "element" || node.type === "custom-element";
+    if (!isInspectableElement || !node.position) {
+      return;
     }
-  }
-  let bodyNode = null;
-  const findBody = (node) => {
-    if (node.type === "element" && node.name === "body") {
-      return node;
+    if (/^[A-Z]/.test(node.name)) {
+      return;
     }
-    if (node.children && Array.isArray(node.children)) {
-      for (const child of node.children) {
-        const found = findBody(child);
-        if (found) return found;
-      }
+    if (node.type === "element" && ["body", "script", "style"].includes(node.name)) {
+      return;
     }
-    return null;
-  };
-  bodyNode = findBody(ast.ast);
-  const rootToWalk = bodyNode || ast.ast;
-  walkAst(rootToWalk, (node) => {
-    if (node.type === "element" && node.position) {
-      if (node.name && /^[A-Z]/.test(node.name)) {
-        return;
-      }
-      if (node.name === "script" || node.name === "style") {
-        return;
-      }
-      if (node.name === "body") {
-        return;
-      }
-      const { line, column } = getLineAndColumn(node.position.start.offset);
-      const loc = {
-        file: normalizedPath,
-        line,
-        column
-      };
-      const encoded = encodeSourceLocation(loc);
-      let tagStart = node.position.start.offset;
-      const tagPrefix = `<${node.name}`;
-      while (tagStart > 0 && code.slice(tagStart, tagStart + tagPrefix.length) !== tagPrefix) {
-        tagStart--;
-      }
-      if (tagStart === 0 && code.slice(0, tagPrefix.length) !== tagPrefix) {
-        return;
-      }
-      let searchEnd = tagStart;
-      let depth = 0;
-      for (let i = tagStart; i < code.length; i++) {
-        if (code[i] === "<") depth++;
-        if (code[i] === ">") {
-          depth--;
-          if (depth === 0) {
-            searchEnd = i;
-            break;
-          }
-        }
-      }
-      const openingTagContent = code.slice(tagStart, searchEnd + 1);
-      const tagPattern = new RegExp(`^<${node.name}([\\s>])`);
-      const match = openingTagContent.match(tagPattern);
-      if (match) {
-        const insertOffset = tagStart + match[0].length - 1;
-        injections.push({
-          offset: insertOffset,
-          attribute: ` data-astro-grab="${encoded}"`
-        });
-      }
+    const { line, column } = node.position.start;
+    const tagStart = getCharacterOffset(lineStartOffsets, line, column);
+    const tagPrefix = `<${node.name}`;
+    if (code.slice(tagStart, tagStart + tagPrefix.length) !== tagPrefix) {
+      return;
     }
+    const insertOffset = tagStart + tagPrefix.length;
+    if (injectionOffsets.has(insertOffset)) {
+      return;
+    }
+    const encoded = encodeSourceLocation({
+      file: normalizedPath,
+      line,
+      column
+    });
+    injectionOffsets.add(insertOffset);
+    injections.push({
+      offset: insertOffset,
+      attribute: ` data-astro-grab="${encoded}"`
+    });
   });
-  injections.sort((a, b) => b.offset - a.offset);
-  let instrumentedCode = code;
-  for (const { offset, attribute } of injections) {
-    instrumentedCode = instrumentedCode.slice(0, offset) + attribute + instrumentedCode.slice(offset);
-  }
+  injections.sort(
+    (firstInjection, secondInjection) => secondInjection.offset - firstInjection.offset
+  );
+  const instrumentedCode = injections.reduce(
+    (currentCode, injection) => currentCode.slice(0, injection.offset) + injection.attribute + currentCode.slice(injection.offset),
+    code
+  );
   return { code: instrumentedCode };
 };
-var walkAst = (node, callback) => {
-  if (!node || typeof node !== "object") {
-    return;
-  }
-  callback(node);
-  if (node.children && Array.isArray(node.children)) {
-    for (const child of node.children) {
-      walkAst(child, callback);
+var getLineStartOffsets = (code) => {
+  const lineStartOffsets = [0];
+  for (let characterIndex = 0; characterIndex < code.length; characterIndex++) {
+    if (code[characterIndex] === "\n") {
+      lineStartOffsets.push(characterIndex + 1);
     }
   }
-  if (node.attributes && Array.isArray(node.attributes)) {
-    for (const attr of node.attributes) {
-      walkAst(attr, callback);
+  return lineStartOffsets;
+};
+var getCharacterOffset = (lineStartOffsets, line, column) => {
+  const lineStartOffset = lineStartOffsets[line - 1];
+  if (lineStartOffset === void 0) {
+    return -1;
+  }
+  return lineStartOffset + column - 1;
+};
+var getRelativeFilePath = (filePath, root) => {
+  let normalizedPath = normalizePath(filePath);
+  if (!root) {
+    return normalizedPath;
+  }
+  const normalizedRoot = normalizePath(root);
+  if (!normalizedPath.startsWith(normalizedRoot)) {
+    return normalizedPath;
+  }
+  normalizedPath = normalizedPath.slice(normalizedRoot.length);
+  return normalizedPath.startsWith("/") ? normalizedPath.slice(1) : normalizedPath;
+};
+var findBody = (node) => {
+  if (node.type === "element" && node.name === "body") {
+    return node;
+  }
+  if (!("children" in node)) {
+    return null;
+  }
+  for (const childNode of node.children) {
+    const bodyNode = findBody(childNode);
+    if (bodyNode) {
+      return bodyNode;
     }
   }
+  return null;
+};
+var walkAst = (node, callback) => {
+  callback(node);
+  if (!("children" in node)) {
+    return;
+  }
+  node.children.forEach((childNode) => walkAst(childNode, callback));
 };
 
 // src/server/transform.ts
@@ -230,21 +212,47 @@ var transformAstroFile = async (code, id, root) => {
     return { code };
   }
 };
+var SourceOutsideRootError = class extends Error {
+  constructor(file) {
+    super(`Source file must be inside project root: "${file}"`);
+    this.name = "SourceOutsideRootError";
+  }
+};
+var isPathInsideRoot = (root, file) => {
+  const relativePath = relative(root, file);
+  return relativePath !== ".." && !relativePath.startsWith(`..${sep}`) && !isAbsolute(relativePath);
+};
+var resolveSourceFile = async (root, sourceFile) => {
+  const resolvedRoot = await realpath(root);
+  const candidatePath = resolve(resolvedRoot, sourceFile);
+  if (!isPathInsideRoot(resolvedRoot, candidatePath)) {
+    throw new SourceOutsideRootError(sourceFile);
+  }
+  const resolvedFile = await realpath(candidatePath);
+  if (!isPathInsideRoot(resolvedRoot, resolvedFile)) {
+    throw new SourceOutsideRootError(sourceFile);
+  }
+  return resolvedFile;
+};
 var handleSnippetRequest = async (src, options) => {
   const { root, contextLines = 4 } = options;
   let loc;
   try {
-    loc = decodeSourceLocation(decodeURIComponent(src));
+    loc = decodeSourceLocation(src);
   } catch (error) {
     throw new Error(
       `Invalid source location: ${error instanceof Error ? error.message : String(error)}`
     );
   }
-  const filePath = resolve(root, loc.file);
+  let filePath;
   let content;
   try {
+    filePath = await resolveSourceFile(root, loc.file);
     content = await readFile(filePath, "utf-8");
   } catch (error) {
+    if (error instanceof SourceOutsideRootError) {
+      throw error;
+    }
     throw new Error(
       `Failed to read file "${loc.file}": ${error instanceof Error ? error.message : String(error)}`
     );
@@ -365,7 +373,7 @@ var astroGrabVitePlugin = (options = {}) => {
 // src/server/snippet-handler-content.ts
 async function handleSnippetRequestFromContent(src, content, options = {}) {
   const { contextLines = 4 } = options;
-  const loc = decodeSourceLocation(decodeURIComponent(src));
+  const loc = decodeSourceLocation(src);
   const { snippet, startLine, endLine } = extractSnippet(
     content,
     loc.line,
diff --git a/dist/types-C9qZXkxT.d.ts b/dist/types-C9qZXkxT.d.ts
index 677989cdd3be3d74521e86c75f833c5b47e77060..ef4cc4537830634c0e2952dc33001e81ec64fbde 100644
--- a/dist/types-C9qZXkxT.d.ts
+++ b/dist/types-C9qZXkxT.d.ts
@@ -27,7 +27,11 @@ interface ClientConfig {
     debug?: boolean;
     /** Base URL path for API endpoints (e.g., '/api/astro-grab') */
     apiBaseUrl?: string;
-    /** Template for clipboard output using {{variable}} interpolation */
+    /**
+     * Template for clipboard output using {{variable}} interpolation.
+     * Placeholders: file, snippet, startLine, endLine, targetLine, language,
+     * trace (component chain from the targeted element outwards, max 4 entries).
+     */
     template?: string;
 }
 /**
@@ -50,7 +54,11 @@ interface AstroGrabOptions {
     debug?: boolean;
     /** Enable/disable the dev toolbar app (default: true) */
     toolbar?: boolean;
-    /** Template for clipboard output using {{variable}} interpolation */
+    /**
+     * Template for clipboard output using {{variable}} interpolation.
+     * Placeholders: file, snippet, startLine, endLine, targetLine, language,
+     * trace (component chain from the targeted element outwards, max 4 entries).
+     */
     template?: string;
 }
 
diff --git a/package.json b/package.json
index ec169068e861464783739260d7ecf0f669c4d499..18ab00949953703a7771e54aa1cb2abbadb0cc71 100644
--- a/package.json
+++ b/package.json
@@ -51,7 +51,7 @@
     "vitest": "^2.1.0"
   },
   "peerDependencies": {
-    "astro": "^5.0.0"
+    "astro": "^5.0.0 || ^6.0.0 || ^7.0.0"
   },
   "publishConfig": {
     "access": "public"
